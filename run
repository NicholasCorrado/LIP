make

query=$1

if [ "$#" -ne 1 ]; then
    query=all
fi

echo $query


for i in `seq 1 10`;
do
	echo $i
	echo "hash"
	./csv_to_arrow $query hash y > scripts/data/hash_enum_$i
	echo "lip"
	./csv_to_arrow $query lip y > scripts/data/lip_enum_$i
done 


# python3 ./scripts/plot.py